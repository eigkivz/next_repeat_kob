generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Apartment {
  id        String   @id @default(cuid())
  name      String
  phone     String
  address   String
  email     String?
  lineId    String?
  taxCode   String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model RoomType {
  id        String   @id @default(cuid())
  name      String
  price     Int      @default(0)
  remark    String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  status    String   @default("active")
  rooms     Room[]
}

model Room {
  id          String    @id @default(cuid())
  name        String
  towerName   String
  totalLevel  Int
  totalRoom   Int
  roomTypeId  String
  remark      String?
  status      String    @default("active")
  statusEmpty String    @default("empty") // empty, notempty
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  roomType    RoomType  @relation(fields: [roomTypeId], references: [id])
  bookings    Booking[]
}

model Booking {
  id              String    @id @default(cuid())
  customerName    String
  customerPhone   String
  customerAddress String
  customerCardId  String
  customerGender  String
  status          String    @default("active") // active, inactive
  stayAt          DateTime
  stayUntil       DateTime?
  deposit         Int       @default(0) // มัดจำ
  remark          String?
  roomId          String
  room            Room      @relation(fields: [roomId], references: [id])
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt
}
